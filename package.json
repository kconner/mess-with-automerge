{
  "name": "mess-with-automerge",
  "private": "true",
  "scripts": {
    "build": "tsc --strict index.ts",
    "build:watch": "tsc --watch --strict index.ts",
    "start": "node index.js",
    "start:watch": "nodemon '*.js' --quiet -- npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@automerge/automerge": "^2.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "typescript": "^4.9.5"
  }
}
